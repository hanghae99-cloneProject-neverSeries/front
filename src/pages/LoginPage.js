// * import Basic
import React from "react";

// * import Components
import { Text, Grid, Image } from "../elements/index";

const LoginPage = (props) => {
    return <React.Fragment>로그인페이지</React.Fragment>;
};

export default LoginPage;
